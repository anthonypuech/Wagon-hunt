<h1> All products</h1>
<p><%= link_to "Revenir à l'acceuil", root_path, class: "btn btn-primary" %></p>
<p><%= link_to "Ajouter un produit", new_product_path, class: "btn btn-danger" %></p>
<ul>
  <% @products.each do |product| %>
  <li>
    <%= link_to product.name, product_path(product)%> (<%= product.url %>)
  </li>

  <% end %>
</ul>
